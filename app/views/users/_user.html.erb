<li>
	<%= gravatar_for(user, size:20) %>
	<%= link_to user.name, user %>
	<% if current_user.admin? && !correct_user(user) %>
		| <%= link_to "delete", user, method: :delete, data: { confirm: "Yor are sure" } %>
	<% end %>
</li>